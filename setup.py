from setuptools import setup

requirements = [
      'gym',
      'chainer',
      'numpy',
      'Pillow']

setup(name='textlocalizationenvironment',
      version='0.1.0',
      install_requires=requirements
)
